extends layout

block content
  h1= title

  ul
    each genre in genre_list
      li
        a(href=genre.url) #{genre.name}
    
    else
      li There are no genres yet. Create a new genre
        a(href="../genre/create") here.